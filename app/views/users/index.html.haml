%h1 Welcome to the Mission Board
%h3 Please select your user or create a new one to view the MissionBoard
- if @users.present?
  %ul
    - @users.each do |user|
      %li= link_to user.name, boards_url
 
/ = render 'form', user: @user

= render 'form', user: @user


/ %table
/   %tbody
/     %tr
/       %td Name
/       %td Photo
/     %tr 
/       %td users.name
/     %tr
/       %td link_to "Create", create_user_path
